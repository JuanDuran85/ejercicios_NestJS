version: '3.8'

services:
  postgres-db:
    image: postgres:17
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
    container_name: advanced_db
    volumes:
      - ./postgres-db:/var/lib/postgresql/data
    ports:
      - 5432:5432